with entry{
    num = [1, 2, 3, 4, 5];
    for i in range(0, len(num)) {
        print(num[i]);
    }
}

# Loop this name  5 times
with entry{
    name = ["ANTHONY", "DAVID", "JOHN", "MICHAEL", "JAMES"];
    for i in range(0, 5) {
        print(name[i]);
    }
}